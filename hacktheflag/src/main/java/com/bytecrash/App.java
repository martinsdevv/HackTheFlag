package com.bytecrash;

import com.bytecrash.terminal.TerminalSimulator;

public class App {
    public static void main(String[] args) {
        TerminalSimulator terminal = new TerminalSimulator();
        terminal.start();
    }
}